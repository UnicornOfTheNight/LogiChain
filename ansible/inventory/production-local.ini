# Inventaire utilise par le runner GitHub Actions self-hoste (installe
# directement sur le VPS cible, voir ansible/README.md). Connexion locale :
# pas de SSH, le runner execute Ansible sur la machine qu'il provisionne.
[production]
logichain-vps ansible_host=187.127.84.96 ansible_connection=local

[production:vars]
ansible_python_interpreter=/usr/bin/python3
