[production]
# Rempli après bootstrap.yml : ansible_user = le deploy_user créé,
# ansible_ssh_private_key_file = la clé privée correspondante (jamais la .pub).
logichain-vps ansible_host=187.127.84.96 ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/logichain_deploy_ed25519

[production:vars]
ansible_python_interpreter=/usr/bin/python3
