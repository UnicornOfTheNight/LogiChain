# vault.yml est chiffré (Ansible Vault) : il DOIT etre commite, c'est
# le mot de passe de dechiffrement (ANSIBLE_VAULT_PASSWORD) qui reste secret.
*.retry
.vault_pass
*.pem
id_*
!id_*.pub
*_ed25519
!*_ed25519.pub
*_rsa
!*_rsa.pub
